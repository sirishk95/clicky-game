import React from "react";

const Header = props => (
    <header className="header">
        <h1>Clicky Game!</h1>
        <h2> 
            Click on an image to earn points
        </h2>
    </header>

);

export default Header;